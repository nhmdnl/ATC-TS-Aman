node_modules/
dist/
dist-electron/
release/
.DS_Store
*.log

# Licensed chart material (Navigraph/Jeppesen approach plates etc.) —
# keep source charts here for reference; they must NEVER be committed
# or redistributed. Only independently-stated simulation parameters
# belong in the source tree.
charts/
*.pdf

# Local tool/runtime state — not project source.
# NOTE: do not add .github/ here — CI workflows are versioned project source.
.claude/
.docs/
.impeccable/
.omp/
.qwen/
.release/
.remember/
.superpowers/
.tokensave/
.vscode/
bun.lock
graphify-out/

# Agent / skill output directories — regenerated on demand, never source
brag-output/

# Scratch working copies that must not reach the build.
# A second .airport file declaring an ICAO that hhas.airport.json already
# owns produced duplicate briefing-picker entries (see TASKS.md escalations).
*2.airport
Tower-Simulator-3-Reproduction-Spec.md
